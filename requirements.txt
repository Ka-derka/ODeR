PySide6==6.11.2; sys_platform != "darwin"
PySide6==6.9.3; sys_platform == "darwin"
requests>=2.31
# PyPI publishes the native binding for Windows/Linux. macOS release builds
# use Homebrew's matching 2.1.1 bottle and Python binding (see build_macos.sh).
libtorrent==2.1.1; sys_platform != "darwin"
